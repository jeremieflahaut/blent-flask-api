[project]
name = "flask-api"
version = "0.1.0"
description = "API REST e-commerce DigiMarket (Flask + SQLAlchemy + JWT) — projet Blent"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pyjwt>=2.13.0",
    "sqlalchemy>=2.0.50",
]
